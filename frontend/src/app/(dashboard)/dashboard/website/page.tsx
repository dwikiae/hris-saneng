import { InfoPanel } from "@/components/core/platform/InfoPanel";

export default function WebsitePage() {
  return (
    <InfoPanel
      titleKey="website.title"
      descriptionKey="website.description"
      bodyKey="website.body"
    />
  );
}

import { InfoPanel } from "@/components/core/platform/InfoPanel";

export default function LogoutPage() {
  return (
    <InfoPanel titleKey="logout.title" descriptionKey="logout.description" bodyKey="logout.body" />
  );
}

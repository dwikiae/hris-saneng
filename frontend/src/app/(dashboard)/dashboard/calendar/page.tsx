import { InfoPanel } from "@/components/core/platform/InfoPanel";

export default function CalendarPage() {
  return (
    <InfoPanel
      titleKey="calendar.title"
      descriptionKey="calendar.description"
      bodyKey="calendar.body"
    />
  );
}

import { InfoPanel } from "@/components/core/platform/InfoPanel";

export default function AssetsPage() {
  return (
    <InfoPanel titleKey="assets.title" descriptionKey="assets.description" bodyKey="assets.body" />
  );
}

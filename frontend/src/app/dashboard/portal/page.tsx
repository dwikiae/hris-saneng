import { InfoPanel } from "@/components/core/platform/InfoPanel";

export default function PortalPage() {
  return (
    <InfoPanel
      titleKey="portal.title"
      descriptionKey="portal.description"
      bodyKey="portal.body"
    />
  );
}

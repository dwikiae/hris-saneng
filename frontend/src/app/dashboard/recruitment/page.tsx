import { InfoPanel } from "@/components/core/platform/InfoPanel";

export default function RecruitmentPage() {
  return (
    <InfoPanel
      titleKey="recruitment.title"
      descriptionKey="recruitment.description"
      bodyKey="recruitment.body"
    />
  );
}
